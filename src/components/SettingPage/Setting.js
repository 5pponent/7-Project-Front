import { Box, Stack, Typography } from "@mui/material";


export default function Setting(props) {
	
	
	return (
		<Box sx={{ width: '600px', margin: '0 auto' }} p={5}>
			<Stack>
				<Typography variant="h5">기본 설정</Typography>
			</Stack>
		</Box>
	);
}